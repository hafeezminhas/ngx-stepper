import { ProgStepDirective } from './prog-step.directive';

describe('ProgStepDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgStepDirective();
    expect(directive).toBeTruthy();
  });
});
